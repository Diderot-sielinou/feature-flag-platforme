{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Permet à ESLint d'utiliser les fichiers de test
    "rootDir": "./"
  },
  "include": ["src/**/*", "test/**/*", "**/*.ts"],
  // Surcharge l'exclusion du dossier 'test' héritée de tsconfig.json
  "exclude": ["node_modules", "dist"]
}
